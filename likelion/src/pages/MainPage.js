import MainForm from '../containers/MainForm';

const MainPage = () => {
    return (
        <div>
            <MainForm>
            </MainForm>
        </div>
    );
};

export default MainPage;
